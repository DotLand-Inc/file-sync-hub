name: CI Build & Test

on:
  push:
    branches:
      - develop
  pull_request:
  workflow_call:
    inputs:
      push_images:
        description: 'Push images to registry'
        default: false
        required: false
        type: boolean
      image_tag:
        description: 'Tag to use for images'
        default: 'develop'
        required: false
        type: string
    secrets:
      DOCKER_PAT:
        required: true

env:
  DOCKER_USERNAME: hsaii
  WEB_IMAGE: hsaii/dms-api

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore file-sync-hub.sln

      - name: Build
        run: dotnet build file-sync-hub.sln --no-restore --configuration Release

      # Skip test step as there are no test projects in the solution yet
      # Uncomment when test projects are added
      # - name: Test
      #   run: dotnet test file-sync-hub.sln --no-build --configuration Release --verbosity normal

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: ${{ inputs.push_images }}
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PAT }}

      - name: Build and Push Web API
        uses: docker/build-push-action@v5
        with:
          context: ./
          file: ./Dockerfile
          push: ${{ inputs.push_images == true }}
          tags: ${{ env.WEB_IMAGE }}:${{ inputs.image_tag || 'develop' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            ASPNETCORE_ENVIRONMENT=Production
