# PostgreSQL Configuration
POSTGRES_DB=filesyncdb
POSTGRES_USER=filesync
POSTGRES_PASSWORD=filesync_password
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PASSWORD=redis_password
REDIS_PORT=6379
REDIS_MAX_MEMORY=256mb

# pgAdmin Configuration (Optional - for database management UI)
PGADMIN_EMAIL=admin@filesync.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Redis Commander Configuration (Optional - for Redis management UI)
REDIS_COMMANDER_PORT=8081

# Application Configuration
# Connection string format for .NET application:
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=filesyncdb;Username=filesync;Password=filesync_password
# Redis__ConnectionString=localhost:6379,password=redis_password
